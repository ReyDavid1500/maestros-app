import { View, Text } from "react-native";

/**
 * Pantalla de registro de cliente.
 * TODO Fase 02: implementar formulario completo.
 */
export default function RegisterScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center px-6">
      <Text className="text-2xl font-inter-bold text-text mb-4">
        Crear cuenta
      </Text>
      <Text className="text-text-secondary font-inter">
        — Pantalla en construcción (Fase 02) —
      </Text>
    </View>
  );
}
